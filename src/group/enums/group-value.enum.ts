export enum typeEnum {
  static = "static",
  dynamic = "dynamic",
}

export enum statusEnum {
  publish = "publish",
  unpublish = "unPublish",
  draft = "draft",
}

export enum mediumOfInstruction {
  hindi = "hindi",
  english = "english",
}
